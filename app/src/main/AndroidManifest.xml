<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.healtheye">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/myAppTheme">
        <activity android:name=".View.Activities.updateProfileActivity"></activity>
        <activity android:name=".View.Activities.addProfileActivity" />
        <activity android:name=".View.Activities.SearchFoodActivity" />
        <activity android:name=".View.Activities.registrationActivity" />
        <activity
            android:name=".View.Activities.UserMainActivity"
            android:label="@string/title_activity_user" />
        <activity android:name=".View.Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".liteClassifier.ClassifierActivity" />
        <activity android:name=".View.Activities.displayFoodActivity" />
    </application>

</manifest>